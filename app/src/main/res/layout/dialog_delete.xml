<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:gravity="center">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:background="@color/colorMainNormal"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="选取要执行的操作"
            android:textColor="#000000"
            android:textSize="20sp"/>

        <Button
            android:id="@+id/dialog_delete_one"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/save_outcome_selector"
            android:text="只删除此记录"
            android:textColor="#ffffff"/>

        <Button
            android:id="@+id/dialog_delete_behind"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/save_outcome_selector"
            android:text="删除且不再自动添加固定记录"
            android:textColor="#ffffff"/>

        <Button
            android:id="@+id/dialog_delete_all"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/save_outcome_selector"
            android:text="删除所有固定记录"
            android:textColor="#ffffff"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/dialog_close"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentRight="true"
        android:src="@drawable/close"/>

</RelativeLayout>